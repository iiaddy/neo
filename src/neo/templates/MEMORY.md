# MEMORY.md — long-term memory for neo

> This file is read automatically at the start of every session, and the
> agent updates it when you tell it to remember something or when it learns
> a durable fact about this project. Keep entries short — one fact per line.
> Never store secrets, tokens, or passwords here.

## Facts

- (example) Tests run with `pytest -q`; the suite takes ~3 minutes.

## Decisions

- (example) Chose Textual over Rich-only TUI for the interactive chat.

## Gotchas

- (example) The staging API is slow on first request (~30s cold start).
