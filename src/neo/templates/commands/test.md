---
description: Run the project's test suite and fix failures.
---

Run the project's tests (check AGENTS.md for the exact command; try the
obvious ones if it's missing). If anything fails:
1. Read the failure output fully.
2. Fix the root cause in the code (not the test, unless the test is wrong).
3. Re-run until green.
Report: what you ran, what failed, what you changed. Arguments: $ARGUMENTS
