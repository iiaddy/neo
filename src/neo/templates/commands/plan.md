---
description: Enter plan mode — research and design without writing code.
---

Enter plan mode for the following goal:

$ARGUMENTS

Call the `plan_enter` tool with `goal` set to the text above (condensed to
one sentence if needed). While in plan mode, research the codebase with
read-only tools, write the plan document to `.neo/plans/<slug>.md`, and
finish with `plan_exit`. Do not write or run any code until the plan is
approved.
